# Render 部署與活動準備 —— 完整待辦

從現在到活動結束，你要做的所有事。照順序做，每項都可以打勾。

---

## 階段 0：現在就能做（不需要任何 API key）

- [ ] **建立 GitHub repo 並推上去**

  ```bash
  git init
  git add .
  git commit -m "會議即時英譯中字幕系統"
  ```

  然後在 GitHub 建一個 repo，把 remote 加上去 push。

  ⚠️ push 前確認 `.gitignore` 有這三行（已經幫你寫好了）：
  ```
  .env
  node_modules/
  transcripts/
  ```
  這樣 API key 和逐字稿都不會上傳。

- [ ] **註冊 Render 帳號** — <https://render.com>，用 GitHub 登入最快

---

## 階段 1：申請兩把 API key

- [ ] **Deepgram** — <https://console.deepgram.com>
  - 註冊後新帳號送 **US$200** 額度
  - 建 API key，權限至少要 Member（發放短期 token 需要）
  - 你的活動預估只會用掉約 US$19

- [ ] **Gemini** — <https://aistudio.google.com/apikey>
  - 用 Google 帳號登入
  - 點「Create API key」，會自動幫你建一個 Google Cloud 專案
  - 不需要信用卡

- [ ] **想好兩組自訂密碼**
  - `ROOM_CODE` — 給觀眾輸入的，建議短好念，例如 `SHOW`、`AI26`
  - `OPERATOR_KEY` — 只給你自己，建議長一點，例如 `conf2026-operator-x7k2`

---

## 階段 2：部署到 Render

- [ ] Render → **New → Blueprint**
- [ ] 選你剛才推上去的 repo
- [ ] Render 讀到 `render.yaml` 後會**跳出來問你四個變數**，依序填入：

  | 變數 | 填什麼 |
  | --- | --- |
  | `DEEPGRAM_API_KEY` | 階段 1 拿到的 |
  | `GEMINI_API_KEY` | 階段 1 拿到的 |
  | `ROOM_CODE` | 你想的房間碼 |
  | `OPERATOR_KEY` | 你想的操作金鑰 |

- [ ] 按下部署，等 2～3 分鐘
- [ ] 記下網址：`https://__________.onrender.com`

---

## 階段 3：部署後立刻驗證（10 分鐘）

- [ ] **健康檢查** — 瀏覽器開 `https://你的網址/healthz`

  應該看到 `{"ok":true,...}`

- [ ] **開收音頁** — `https://你的網址/captioner`
  - 填入 `OPERATOR_KEY`
  - 確認頂端**沒有紅色警告**（有的話代表某個 key 沒設好）
  - 確認引擎顯示「自動（優先 Deepgram）」

- [ ] **按「開始收音」**，允許麥克風權限
  - 對著麥克風講幾句英文
  - 確認左邊出現英文、右邊出現**繁體中文**

- [ ] **檢查中文是不是台灣用語** — 故意講含 software、network、data 的句子，
      應該翻成「軟體、網路、資料」而不是「軟件、網絡、數據」

- [ ] **用手機開觀眾網址** — `https://你的網址/?room=你的房間碼`
  - 確認手機看得到字幕
  - 測「英文」開關、「A+ / A−」字級

- [ ] **測房間碼錯誤** — 故意輸入錯的，應該顯示「房間碼錯誤」

- [ ] **測下載逐字稿** — 收音頁按「下載逐字稿」，確認檔案內容正確

- [ ] **測重啟韌性** — 在 Render 後台按 **Manual Deploy → Restart service**
  - 手機上的字幕頁應該幾秒後自動重連
  - 收音頁的「💾 N 句」計數**不應該歸零**（那份存在你瀏覽器裡）

---

## 階段 4：活動前準備

- [ ] **填詞彙表** — 編輯 `glossary.json`，把講者姓名、公司名、專有名詞都加進去

  ```json
  { "terms": [
      { "en": "Dr. Jane Smith", "zh": "" },
      { "en": "quantum annealing", "zh": "量子退火" }
  ]}
  ```
  `zh` 留空 = 字幕保留英文原文。
  改完 push 到 GitHub，Render 會**自動重新部署**。

  > 這個表同時餵給翻譯提示詞和 Deepgram 的 keyterm，**兩邊都會變準**。七天下來同一個詞翻法一致，這項的效益很高。

- [ ] **決定要不要升級**
  - 免費方案閒置 15 分鐘會休眠，喚醒約 1 分鐘
  - 不想管 → Render 後台把 plan 改成 **Starter（$7／月）**，永不休眠
  - 活動結束記得降回來或刪掉服務

- [ ] **印房間碼小卡** — 寫上網址和房間碼，發給觀眾。
      建議也做一張 QR code，現場用手機掃比打字快很多。

- [ ] **準備硬體**
  - 指向性麥克風（**收音品質直接決定字幕品質**，這是整條管線最前端）
  - 收音端用**有線網路**
  - 筆電接電源
  - 備用手機可以開熱點

---

## 階段 5：彩排（活動前一天，務必做）

- [ ] 在**真實會場**，用**會場的網路**
- [ ] **10 台裝置同時連線**
- [ ] 請講者實際講一段，量測「講完到中文字幕出現」要幾秒
- [ ] 中途**拔掉一次網路線**再插回去，確認三處自動重連都有作用
- [ ] 確認 Deepgram 額度還夠（<https://console.deepgram.com> 看用量）
- [ ] 演練一次「Deepgram 掛掉」：收音頁把引擎改成「只用 Web Speech 備援」，
      確認還是能出字幕（品質較差但撐得住）

---

## 階段 6：活動每一天的固定流程

**開場前**
- [ ] 提前 2 分鐘打 `https://你的網址/healthz` 把服務叫醒（升 Starter 的話跳過）
- [ ] 開收音頁，確認操作金鑰還在，按「開始收音」
- [ ] 自己講一句測試，確認字幕有出來

**進行中**
- [ ] 注意頂端的「👥 N 位觀眾」，數字對不對
- [ ] 注意「💾 N 句」有在增加

**散會後**
- [ ] **按「下載逐字稿」** ← 這是唯一每天一定要記得的事

  Render 免費方案沒有持久磁碟，伺服器重啟後 `transcripts/` 會消失。
  可靠的那份在你的瀏覽器裡，按下載才會變成檔案。

---

## 出事時怎麼辦

| 症狀 | 先檢查 |
| --- | --- |
| 觀眾說看不到字幕 | 收音頁的「👥」有沒有算到他？房間碼對不對？ |
| 字幕只有英文沒中文 | Gemini key 是否失效／額度用完，看 Render 的 Logs |
| 完全沒有字幕 | 收音頁頂端的紅色橫幅寫什麼；音量表有沒有在動 |
| 字幕延遲很久 | 會場網路。收音端改用有線 |
| Deepgram 一直斷 | 引擎切成「只用 Web Speech 備援」先撐著 |
| 第一個觀眾連不上 | 服務在休眠，打一次 `/healthz` 等 1 分鐘 |
| 服務整個掛掉 | Render 後台 Logs 看錯誤；真的不行就退回本機跑，觀眾連區網 IP |

**最後的保險**：真的全掛了，收音頁瀏覽器裡的逐字稿還在，會後還是能提供完整中英文紀錄。

---

## 花費總結

| 項目 | 金額 |
| --- | --- |
| Deepgram | US$0（$200 額度吃掉約 $19） |
| Gemini | 約 US$0～5，多半在免費額度內 |
| Render 免費方案 | US$0 |
| Render Starter（選配，免休眠） | US$7／月 |
| **合計** | **US$0～12（約 NT$0～380）** |
